import keyMirror from 'keymirror';

export const EMPTY_CHAR = ' ';
export const NULL = '';

export const modes = keyMirror({
  RESIZE: null,
  TYPE: null,
  SELECT: null,
  DRAW: null,
  MODE: null,
});
